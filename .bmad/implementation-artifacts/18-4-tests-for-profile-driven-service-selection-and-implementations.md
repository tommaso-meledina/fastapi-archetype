# Story 18.4: Tests for Profile-Driven Service Selection and Implementations

Status: done

## Story

As a **software engineer**, I want **tests that verify profile-driven selection and the behaviour of default and mock dummy implementations**, so that **regressions are caught and the PoC is validated**.

## Acceptance Criteria

- Tests for PROFILE=default (existing endpoint tests); tests for PROFILE=mock (v1 and v2 list/create/put); unit tests for default and mock implementations; full suite pass; coverage ≥90%; tests use dependency_overrides or PROFILE as appropriate.
